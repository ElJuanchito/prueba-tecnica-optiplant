package com.optiplant.inventory.transfers.domain.model;

/** Listing filter (contract §6): whether the caller's own branch is the origin or the destination. */
public enum TransferDirection {
	INBOUND, OUTBOUND
}
