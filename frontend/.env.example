# URL del backend a la que el proxy de dev de Vite reenvia las peticiones `/api/*`.
# Opcional: sin este archivo el proxy usa http://localhost:8080, que cubre tanto
# el backend nativo (`make frontend`) como el contenerizado (`make up`).
VITE_BACKEND_URL=http://localhost:8080
